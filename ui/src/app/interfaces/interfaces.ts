export interface subject{
    name: string,
    id: number
    }

export interface list{
    id: number,
    // group_id: number,
    group_name: string,
    subject_id: number,
    subject_name: string,
    name: string,
    // is_public: boolean,
    // semestr: number
    }